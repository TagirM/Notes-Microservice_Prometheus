server:
  port: 8081
#  подключение к базе данных
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/notes-api
    username: bestuser
    password: bestuser

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  sql:
    init:
      mode: always
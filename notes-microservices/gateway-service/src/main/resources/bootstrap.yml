spring:
  application:
    name: gateway-service
    #    Перенаправление на конфиг-сервер с настройками
  cloud:
    config:
      uri: http://localhost:8001
      fail-fast: true
      password: admin
      username: user
